# Dockerfile for Agent Service with Template Method Pattern
# Architecture: BaseAgentService (abstract) + EnhancedAgentService (concrete)
# Security: 4-control agent security pipeline integrated via Template Method
FROM python:3.11-slim

# Set environment variables
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1

# Template Method Security Defaults (can be overridden at runtime)
ENV TEMPLATE_METHOD_SECURITY_ENABLED=true
ENV TEMPLATE_METHOD_SECURITY_LEVEL=HIGH
ENV TEMPLATE_METHOD_REQUEST_FILTERING=true
ENV TEMPLATE_METHOD_RESPONSE_FILTERING=true

# Set work directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements first for better caching
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY . .

# Expose port
EXPOSE 8080

# Health check
HEALTHCHECK --interval=30s --timeout=30s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8080/health || exit 1

# Run the application
CMD ["uvicorn", "agent_service:app", "--host", "0.0.0.0", "--port", "8080"]
